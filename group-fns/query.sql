SELECT
	MIN(SAL)
FROM
	EMP;

SELECT
	MAX(SAL)
FROM
	EMP;

SELECT
	SUM(SAL)
FROM
	EMP;

SELECT
	MAX(SAL)
FROM
	EMP
WHERE
	JOB = 'MANAGER';

SELECT
	TO_CHAR(AVG(SAL), 9999.99)
FROM
	EMP;

SELECT
	ROUND(AVG(SAL), 2)
FROM
	EMP;

SELECT
	COUNT(ENAME)
FROM
	EMP;

SELECT
	COUNT(DISTINCT JOB)
FROM
	EMP;

SELECT
	COUNT(*)
FROM
	EMP;